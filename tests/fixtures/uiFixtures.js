

export const initialState = {
    loading: false,
    msgError: null
}

export const errorState = {
    loading: false,
    msgError: 'Error al cargar formulario!'
}

export const loadingState = {
    loading: true,
    msgError: null
}